# 星座交付协同服务

本项目用于承载多星入轨后的测控协同数据。当前仓库提供一个基础 HTTP 服务、领域样例和数据格式检查，便于后续接入遥测、测控窗口、指令计划与执行回执。

运行服务：

```bash
go run ./cmd/server
```

运行检查：

```bash
go test ./...
```

`fixtures/mission.json` 展示卫星、测控站窗口和遥测包的基础字段。时间均使用带时区的 RFC 3339 字符串，设备序号在所属数据源内单调递增。
